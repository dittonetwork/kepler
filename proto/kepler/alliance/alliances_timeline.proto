syntax = "proto3";
package kepler.alliance;

option go_package = "kepler/x/alliance/types";

message AlliancesTimeline {
  uint64 id = 1;
  repeated string participants = 2; 
  uint64 startBlock = 3; 
  uint64 endBlock = 4; 
  string creator = 5;
}
