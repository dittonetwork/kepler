syntax = "proto3";
package kepler.symbioticadapter.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "kepler/symbioticadapter/v1/params.proto";

option go_package = "kepler/x/symbioticadapter/types";

// GenesisState defines the symbioticadapter module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
