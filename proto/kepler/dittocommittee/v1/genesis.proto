syntax = "proto3";
package kepler.dittocommittee.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "kepler/dittocommittee/v1/params.proto";

option go_package = "kepler/x/dittocommittee/types";

// GenesisState defines the dittocommittee module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
