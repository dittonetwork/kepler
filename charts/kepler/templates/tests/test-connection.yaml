apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kepler.fullname" . }}-test-connection"
  labels:
    {{- include "kepler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kepler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
